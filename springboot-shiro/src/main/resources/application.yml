spring:
  thymeleaf:
    suffix: .html
    cache: false
    prefix: classpath:/templates/

  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shiro_test?useUnicode=true&characterEncoding=utf-8&useSSL=false&serverTimezone=GMT%2B8
    username: root
    password: root

  redis:
    # Redis服务器地址
    host: r-pz5pspe5ujnsluhzrv.redis.rds.aliyuncs.com
    # Redis服务器连接端口
    port: 6379
    # Redis数据库索引（默认为0）
    database: 1
    # Redis服务器连接密码（默认为空）
    password: OUAJjuHQ2oIyviGSaXUmH7rhcziHXBLt
    # 连接超时时间（毫秒）
    timeout: 1000